# 🎓 АВТОМАТИЗОВАНА СИСТЕМА ЗБОРУ, АНАЛІЗУ ТА ОБРОБКИ ІНФОРМАЦІЇ ПРО ХЕРСОНСЬКИЙ ДЕРЖАВНИЙ УНІВЕРСИТЕТ В МЕРЕЖІ ІНТЕРНЕТ

## 📋 Опис проекту

Веб-система для автоматизованого моніторингу, збору та аналізу інформації про Херсонський державний університет у мережі Інтернет. Проект забезпечує парсинг новинних джерел, аналіз контенту за допомогою AI, виявлення дублікатів та генерацію статистики згадувань.

## ✨ Основні можливості

* 🔍 **Автоматичний парсинг** новинних джерел за регулярними виразами
* 🤖 **AI-аналіз контенту** з використанням Google Gemini API
* 📊 **Виявлення дублікатів** через векторне подання тексту (sentence-transformers)
* 🏷️ **Автоматична генерація тегів** для статей
* 📈 **Статистика та графіки** згадувань у часі
* 🌐 **Puppeteer-інтеграція** для роботи з динамічними сайтами
* ⚙️ **Планувальник завдань (Cron)** для повної автоматизації
* 📝 **Детальне логування** всіх операцій
* 🐳 **Docker-ready** — готовий до розгортання в контейнерах

## 🛠️ Технічний стек

* **Backend:** Laravel 12.x (PHP 8.3+)
* **Frontend:** Blade, TailwindCSS, Alpine.js
* **База даних:** MySQL 8.0
* **AI/ML:** Google Gemini API, sentence-transformers (Python)
* **Парсинг:** Puppeteer (Node.js), Readability (PHP), DOMDocument
* **Векторна БД:** ChromaDB
* **Інфраструктура:** Docker Compose
* **Веб-сервер:** Nginx + PHP-FPM
* **Автоматизація:** Cron (Linux) в Docker

## 📦 Передумови

Для запуску вам знадобиться наступне ПЗ:

* Docker Desktop / Engine 24.0+ ([тиць](https://www.docker.com/products/docker-desktop/))
* Git ([клаць](https://git-scm.com/downloads))
* Google Gemini API Key ([хап](https://aistudio.google.com/app/apikey))

### Системні вимоги
* **RAM:** Мінімум 4 GB (рекомендовано 8 GB для AI-моделей)
* **Диск:** Мінімум 5 GB вільного місця
* **ОС:** Windows 10/11 (WSL2), macOS, Linux

---

## 🛠️ Встановлення та налаштування

### 1. Клонування репозиторію

1. Відкрийте термінал (**PowerShell** у Windows або **Terminal** у Linux/macOS).
2. Перейдіть у папку, де ви хочете розмістити проект (команда `cd`):

```bash
# Приклад для Windows:
cd C:\Projects

# Приклад для Linux / macOS:
cd ~/Projects
```

3. Тепер клонуйте репозиторій та обов'язково увійдіть у створену папку:

```bash
git clone https://github.com/026288-sketch/HDU-WebAnalyzer.git
cd HDU-WebAnalyzer
```

### 2. Налаштування середовища (Перед запуском!)

Відкрийте файл `.env.example` у текстовому редакторі.
**Саме на цьому етапі** ви можете змінити назву бази даних, користувача та паролі, які будуть створені Docker-ом. Також тут можна одразу увімкнути Cron (`ENABLE_CRON=true`), якщо це необхідно.

```env
# --- Налаштування Бази Даних (Можна змінити під себе) ---
DB_DATABASE=hdu_analyzer     # Назва бази даних
DB_USERNAME=hdu_user         # Ім'я користувача
DB_PASSWORD=hdu_password     # Пароль (рекомендуємо змінити)
```

### 3. Запуск системи
Збудуйте та запустіть контейнери. 

**Важливо:** Переконайтеся, що у терміналі ви знаходитесь саме в папці проекту (наприклад, `C:\Projects\HDU-WebAnalyzer`).

Виконайте команду:

```bash
docker compose up -d --build
```
Мають запуститися 4 контейнери:
1.  `hdu_laravel` (Laravel + Nginx + PHP-FPM + Cron)
2.  `hdu_mysql` (MySQL 8.0)
3.  `hdu_puppeteer` (Puppeteer Server)
4.  `hdu_similarity` (Python ML Service)

### 4. Очікування ініціалізації (Важливо! ⏳)
Перший запуск може зайняти **5-10 хвилин**, оскільки завантажуються ML-моделі для Python, встановлюються залежності Node.js та Composer.

**Рекомендація:** Бажано перевірити логи контейнерів на наявність помилок. Особливо зверніть увагу на контейнер Laravel. Успішно запущені контейнери будуть відображатися зеленим кольором.

Відкрийте логи контейнера Laravel:
```bash
docker compose logs -f laravel
```

Дочекайтеся повідомлення:
> ✅ Laravel setup complete! Starting Supervisor...

Тільки після цього система повністю готова до роботи.

### 5. Налаштування API ключів
Після першого запуску файл `.env` створиться автоматично (на основі `.env.example`). Відкрийте його та додайте ваш API ключ (контейнери перезапускати не обов'язково, якщо не змінювали налаштування БД):

```env
GEMINI_API_KEY="YOUR_API_KEY"
```

#### ⚡ Важливо про ліміти API (Rate Limits)

Система оптимізована під модель `gemini-2.5-flash-lite`. Для безкоштовного тарифу (Free Tier) діють такі обмеження:
* **RPM (Requests Per Minute):** ~15 запитів на хвилину
* **RPD (Requests Per Day):** ~1,500 запитів на день

💡 *Ліміти можуть змінюватися Google. Перевіряйте актуальні дані в Google AI Studio.*

**Програмні обмеження для безпеки:**
Щоб гарантовано уникнути помилки `429 Too Many Requests` при автоматичній роботі, у консольних командах встановлено жорстке програмне обмеження:
* ✅ 3 статті за один запуск аналізу (`ConsoleAnalizeNodes`)
* ✅ 3 статті за один запуск генерації тегів (`ConsoleGenerateTags`)

📊 **Сумарне навантаження:** 6 запитів за цикл - безпечно вкладається у хвилинний ліміт.

⚠️ **Не збільшуйте ці ліміти в коді, якщо ви використовуєте безкоштовний ключ!** Для обробки великих обсягів даних розгляньте можливість використання платного тарифу Google AI.

---

### 6. Налаштування регулярного виразу (Важливо! ⚠️)
Система шукає новини за ключовими словами. Перед запуском перевірте файл `storage/app/regex.txt`.

**Тестовий варіант (за замовчуванням):**
```text
/\bХерсон(а|у|ом|е|і)?\b/iu
```

**Продакшн варіант (рекомендується):**
```text
/\b(?:ХДУ|Херсонськ(?:ий|ого|ому|им|а|е|і|у|ою)?\s?(?:державн(?:ий|ого|ому|им|а|е|і|у|ою)?\sуніверситет(?:у|ом|і)?|держуніверситет(?:у|ом|і)?))\b/iu
```
> **Без коректного regex-файлу парсер не знайде жодної новини!**
> Протестувати роботу регулярного виразу та парсера можна тут: [http://localhost/parser/test-parser](http://localhost/parser/test-parser)

### 7. ⏰ Автоматизація (Cron)
Система автоматично виконує завдання, якщо в `.env` встановлено `ENABLE_CRON=true`.

**⚠️ Важливо:** Якщо ви змінили цей параметр (`ENABLE_CRON`) у `.env` вже **після** запуску контейнерів, потрібно перезібрати контейнер Laravel:
```bash
docker compose up -d --build laravel
```

**Розклад:**
* `data:workflow` — **кожні 10 хвилин**. Виконує повний цикл роботи: збір нових посилань -> завантаження контенту -> AI аналіз емоцій -> генерація тегів.
* `stats:generate` — **щодня о 01:00**. Агрегує статистику за минулу добу для коректного відображення графіків на дашборді.

**Перевірка статусу:**
```bash
# Чи працює планувальник?
docker compose exec laravel supervisorctl status

# Список запланованих задач
docker compose exec laravel php artisan schedule:list
```

---

### 8. Перший вхід
Відкрийте браузер: [http://localhost](http://localhost)

**Облікові дані:**
* **Email:** `admin@example.com`
* **Пароль:** `admin`

⚠️ **Безпека:** Обов'язково змініть пароль на сторінці профілю ([http://localhost/profile](http://localhost/profile)).

**Зверніть увагу:** При вході вас автоматично перенаправить на **Dashboard**. Тут критично важливо звернути увагу на віджети статусів **Puppeteer Server** та **Python Service**. Вони повинні бути в статусі **Online** (зеленим). В іншому випадку система не зможе парсити сайти та аналізувати текст.

---

## 📚 Використання системи

### Веб-інтерфейс

| Розділ | URL | Опис |
| :--- | :--- | :--- |
| 📊 **Dashboard** | `/dashboard` | Головна панель зі статистикою та графіками |
| 🌐 **Джерела** | `/sources` | Керування джерелами новин (додавання/видалення) |
| 📰 **Контент** | `/content` | Перегляд та редагування зібраних статей |
| 🔧 **Ручний парсинг** | `/parser/links` | Ручний запуск збору посилань та статей |
| 🧪 **Тестування** | `/parser/test-parser` | Тестування парсера для окремих URL |
| 📝 **Логи** | `/parser/logs` | Детальні логи всіх операцій |
| 🔍 **ChromaDB** | `/chroma` | Веб-інтерфейс для роботи з векторною БД |

### Консольні команди (Artisan)
Ви можете виконувати команди напряму через `docker compose exec`:

```bash
# --- Основні команди ---

# Повний цикл (Workflow): Збір -> Аналіз -> Теги -> Статистика
docker compose exec laravel php artisan data:workflow

# Збір тільки посилань
docker compose exec laravel php artisan ConsoleParseLinks

# Завантаження контенту статей
docker compose exec laravel php artisan ConsoleParseArticles

# AI-аналіз (Gemini)
docker compose exec laravel php artisan ConsoleAnalizeNodes

# Генерація тегів
docker compose exec laravel php artisan ConsoleGenerateTags

# Генерація тестової статистики (для графіків)
docker compose exec laravel php artisan stats:test --days=90
```

---

## 🎯 Архітектура

```
┌─────────────────────────────────────────────────────────┐
│                     Nginx (Port 80)                     │
│                  hdu_laravel container                  │
├─────────────────────────────────────────────────────────┤
│  PHP-FPM 8.3  │  Laravel 12  │  Cron Scheduler          │
└────────┬────────────────┬────────────────┬─────────────┘
         │                │                │
    ┌────▼────┐    ┌──────▼──────┐    ┌────▼────────┐
    │  MySQL  │    │  Puppeteer  │    │ Similarity  │
    │  8.0    │    │  (Node.js)  │    │  (Python)   │
    │  3306   │    │    3000     │    │    8000     │
    └─────────┘    └─────────────┘    └─────────────┘
         │                │                │
    hdu_network (Docker Bridge Network)
```

---

## 🔧 Усунення несправностей

**1. Контейнери не запускаються або падають**
Перегляньте загальні логи:
```bash
docker compose logs
```

**2. База даних недоступна**
Перевірте, чи працює MySQL і чи правильні дані в `.env`:
```bash
docker compose restart mysql
```
> 💡 *Порада:* Зручно використовувати **DBeaver** для підключення до бази даних та перегляду таблиць, якщо у вас не встановлено phpMyAdmin.

**3. Puppeteer / Python сервіси не відповідають**
Перевірте їх статус через curl з консолі:
```bash
curl http://localhost:3000/health
curl http://localhost:8000/health
```
...або просто відкрийте ці посилання у браузері.
Якщо відповіді немає — перевірте логи:
```bash
docker compose logs -f puppeteer
docker compose logs -f similarity
```

**4. Помилка "Port already in use"**
Якщо порт 80 зайнятий, ви можете змінити його у файлі `docker-compose.yml`:
```yaml
ports:
  - "8080:80"
```
Альтернативний варіант: можна змінити змінну `APP_PORT=80` (наприклад, на 8080) у файлі `.env`. Але після цього потрібно **обов'язково** перезібрати контейнер Laravel, оскільки `docker-compose.yml` використовує цю змінну при збірці:
```bash
docker compose up -d --build laravel
```

---

## ⚡ Quick Start (TL;DR)

```bash
# 1. Clone
git clone https://github.com/026288-sketch/HDU-WebAnalyzer.git
cd HDU-WebAnalyzer

# 2. Configure 
# Edit .env.example: DB settings(опционально), ENABLE_CRON=true

# 3. Run
docker compose up -d --build

# 4. Add Gemini API Key (in generated .env)

# 5. Edit regex.txt (optional but recommended)

# 6. Open
# http://localhost
# Login: admin@example.com / admin 
```
